services:
  fastapi:
    image : free_chatbot:latest
    working_dir: /app
    volumes:
        - ../:/app
    ports:
        - 5001:5001
    restart: always
    command: uv run uvicorn main:app --host 0.0.0.0 --port 5001 --reload
